# Lychee link checker configuration

# Verifica que los anchors (#secciones) existan en los documentos
include_fragments = true

# Verifica enlaces internos Y externos (no solo internos)
offline = false
